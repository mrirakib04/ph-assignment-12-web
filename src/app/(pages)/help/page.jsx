import HelpClient from "./HelpClient";

export const metadata = {
  title: "Help Center || Care.io",
  description:
    "Get support, browse tutorials, and contact our care team for assistance.",
};

const HelpPage = () => {
  return <HelpClient />;
};

export default HelpPage;
